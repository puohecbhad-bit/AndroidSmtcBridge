# 快速开始

## 1. 手机端

安装 `android-smtc-bridge-arm64-release.apk`，打开 **Media Bridge**：

1. 点“打开通知使用权设置”，允许 Media Bridge 读取通知。
2. 开启 Wi-Fi、蓝牙或两者。
3. 记下界面显示的 IP 地址和 6 位 PIN。

Wi-Fi 与电脑必须处于同一可信局域网。蓝牙模式需要先在 Windows 设置中
完成系统配对，然后在手机应用中点“让电脑发现此手机”。

## 2. Windows 端

Wi-Fi 连接：

```text
smtc-bridge.exe --wifi 手机IP --pin 六位PIN
```

例如：

```text
smtc-bridge.exe --wifi 192.168.1.23 --pin 123456
```

也可以直接粘贴手机界面显示的完整地址：

```text
smtc-bridge.exe --wifi 192.168.1.23:45831 --pin 123456
```

蓝牙连接：

```text
smtc-bridge.exe --bluetooth auto --pin 123456
```

`auto` 会依次尝试 Windows 中已配对的蓝牙设备，也可以把 `auto` 换成手机
名称或蓝牙 MAC 地址。

连接成功后，Android 当前媒体会作为 Windows SMTC 会话出现。Windows 的
播放、暂停、上一首、下一首、停止和进度跳转会回传给手机播放器。按
`Ctrl+C` 退出。

## 3. 重新构建

完整源码永久保存在：

```text
E:\Projects\AndroidSmtcBridge
```

Android（构建目录和缓存必须在 E: 或 F:）：

```text
build-android.cmd E:\AndroidSmtcBuild
```

Windows：

```text
windows\build.cmd E:\AndroidSmtcBuild\windows
```

发布签名密钥位于 `E:\Projects\AndroidSmtcBridge\signing`。不要删除或公开
该密钥，否则以后生成的 APK 无法覆盖已安装版本。

## 注意

- 该工具只同步媒体信息与控制指令，不传输声音。
- Wi-Fi 协议有 PIN 验证但没有加密，请仅在可信网络中使用。
- 某些没有建立标准 Android MediaSession 的播放器不会被检测到。
